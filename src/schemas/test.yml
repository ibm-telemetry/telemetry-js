# yaml-language-server: $schema=./transistor-config.schema.json
version: 1
collect:
    npm:
        dependencies:
    jsx:
        elements:
            allowedAttributeStringValues:
              - "one"
              - "two"

projectId: "1234"
